# Swift Package Manager build artifacts
.build/
.swiftpm/

# Xcode derived data
DerivedData/

# Xcode project wrappers (generated by `xed` / `open Package.swift`)
# Keep *.xcworkspace/contents.xcworkspacedata — needed for xed round-trip
*.xcodeproj
*.xcworkspace
!*.xcworkspace/contents.xcworkspacedata
xcuserdata/
*.xcuserstate

# Package.resolved is intentionally committed for reproducible builds.
# Do NOT add Package.resolved here.

# macOS metadata
.DS_Store
